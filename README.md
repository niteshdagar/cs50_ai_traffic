The Task is to write an AI to identify which traffic sign appears in a photograph, using a tensorflow convolutional neural network for self-driving cars.
we'll need labelled dataset, collection of images already categorized by road signs, we’ll use the German Traffic Sign Recognition Benchmark (GTSRB) dataset,
which contains thousands of images of 43 different kinds of road signs.

